VITE_API_PROXY_TARGET=http://localhost:3000
# Optional: override the frontend API base URL directly.
# In local development, leaving this unset uses the Vite /api proxy.
# VITE_API_BASE_URL=http://localhost:3000/api/v1
