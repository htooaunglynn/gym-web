Requirements:
- Use Next.js 15 (App Router) + React 19 + TypeScript + Tailwind CSS v4
- Make it 100% pixel-perfect (exact same as upload photo)
- Use exact hex colors, font families, font sizes, font weights, line heights, letter spacing from Figma
- Use exact padding, margin, border-radius, box-shadow, gap from photo
- Fully responsive (mobile-first, tablet, desktop breakpoints exactly as in photo)
- Component-based architecture (Header, Hero, Features, CTA, Footer etc. ခွဲပြီး ရေးပါ)
- Clean, readable, well-commented code
- Accessibility (aria labels, proper semantic HTML)
- No placeholder images — use real URLs or keep alt text
- Dark mode support if it exists in photo

Generate the complete code with folder structure.
